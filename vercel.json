{
    "version": 2,
    "name": "Inventory-Shop-management",
    "builds": [
       { 
        
       "src": "index.js",
        "use": "@vercel/node"
     }
    ],
    "routes": [
       { 
         "src": "/(.*)",
         "dest": "/",
         "methods": ["GET", "OPTIONS" , "POST", "PATCH", "PUT", "DELETE"]
        }
    ]
 }